package com.lypgod.fbs.Common.Util;

//import org.joda.time.DateTime;
//import org.joda.time.format.DateTimeFormat;
//import org.joda.time.format.DateTimeFormatter;

public class DateUtils {
//    private static final DateTimeFormatter DATE_TIME_FORMAT = DateTimeFormat.forPattern("yyyyMMddHHmmss");
//    private static final DateTimeFormatter DATE_FORMAT = DateTimeFormat.forPattern("yyyy-MM-dd");
//    private static final DateTimeFormatter FILENAME_DATE_FORMAT = DateTimeFormat.forPattern("yyyyMMdd");
//
//    private DateUtils() { }
//
//    public static Date parseDate(String text) throws IllegalArgumentException{
//        return DATE_FORMAT.parseDateTime(text).toDate();
//    }
//
//    public static Date parseFilenameDate(String text) throws IllegalArgumentException{
//        return FILENAME_DATE_FORMAT.parseDateTime(text).toDate();
//    }
//
//    public static Date getDayStartTime(Date date) {
//        DateTime dateTime = new DateTime(date.getTime());
//        return dateTime.withTimeAtStartOfDay().toDate();
//    }
//
//    public static Date getDayEndTime(Date date) {
//        DateTime dateTime = new DateTime(date.getTime());
//        return dateTime.withHourOfDay(23).withMinuteOfHour(59).withSecondOfMinute(59).withMillisOfSecond(999).toDate();
//    }
}
